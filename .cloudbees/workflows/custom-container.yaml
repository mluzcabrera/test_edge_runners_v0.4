apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: Custom container
on:
  workflow_dispatch:
jobs:
  build-edge:
    steps:
      - name: Say hello
        run: echo "hola"
        uses: mlcb-container2
        shell: sh

  

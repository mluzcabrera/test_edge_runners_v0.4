apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: Custom container
on:
  workflow_dispatch:
jobs:
  build-edge:
    steps:
      - name: Say hello
        run: echo "hola"
        uses: docker://mlcb_container
        shell: sh
    runs-on:
      - self-hosted
  build-edge2:
    steps:
      - name: Say hello
        run: echo "hola"
        uses: mlcb_container
        shell: sh
    runs-on:
      - self-hosted

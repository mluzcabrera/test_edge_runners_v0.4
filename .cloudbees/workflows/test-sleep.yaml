apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: test sleep

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: label1
    steps:
      - name: Say hello
        uses: docker://docker.io/library/golang:1.20.3-alpine3.17
        shell: sh
        run: |
          sleep 120
          echo "hello world"
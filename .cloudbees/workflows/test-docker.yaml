apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: test docker

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: label1
    steps:
      - name: Say hello
        uses: docker://golang:1.20.3-alpine3.17
        shell: sh
        run: |
          echo "hello world"
